---
import type { HTMLAttributes } from "astro/types";
import { cn } from "~/lib/utils";
type Props = HTMLAttributes<"p">;

const { class: className, ...rest } = Astro.props;
---

<p class={cn("mx-auto text-4xl font-medium", className)} {...rest}>
  <slot />
</p>
