---
import type { HTMLAttributes } from "astro/types";
import { cn } from "~/lib/utils";

type Props = HTMLAttributes<"label">;
const { id, class: className, ...rest } = Astro.props;
---

<label for={id} class:list={cn("block select-none font-medium leading-6", className)} {...rest}><slot /></label>
